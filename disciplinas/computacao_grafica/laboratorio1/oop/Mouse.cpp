#include "Mouse.h"

Mouse::Mouse() {
}

void
Mouse::click(int button, int state, int x, int y) {
	// if (!keyboard->pressed()) {
	// 	point = convertToGLCoordenates(x, y);

	// 	if (button == GLUT_LEFT_BUTTON && state == GLUT_DOWN)
	// 		leftButtonClicked = state == GLUT_DOWN;
	// 	else
	// 		leftButtonClicked = false;
	// }
}	

void
Mouse::clicked(int x, int y) {
	// Point point;

	// if (!keyboard->pressed()) {
	// 	point = convertToGLCoordenates(x, y);
	// 	if (leftButtonClicked && checkClickArea(point))
	// 		move(x, y);
	// 	this->point = point;
	// }
}