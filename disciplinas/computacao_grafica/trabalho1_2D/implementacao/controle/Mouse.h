#ifndef MOUSE_H
#define MOUSE_H

class Mouse {
public:
	Mouse();

	void clicar(int, int, int, int);
	// Point* clickMotion(int, int, const int, const int, std::vector<Point*>);
	// bool isLeftButtonClicked(void);
	// bool isMotionClickOnArea(void);
	// Point getPoint(void);
	// Color getColor(void);

private:
	// Point point;
	// Point increasePoint;
	// Color color;
	// bool leftButtonClicked;
	// bool motionClickOnArea;

	// Point* isClickOnArea(std::vector<Point*>, const int, const int);
	// float min(float a, float b);
	// float max(float a, float b);
};

#endif